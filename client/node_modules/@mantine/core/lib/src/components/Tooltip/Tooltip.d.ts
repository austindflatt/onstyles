import React from 'react';
import { DefaultProps, MantineColor, ClassNames, MantineMargin } from '@mantine/styles';
import { SharedPopperProps } from '../Popper/Popper';
import useStyles from './Tooltip.styles';
export declare type TooltipStylesNames = ClassNames<typeof useStyles>;
export interface TooltipProps extends Omit<DefaultProps<TooltipStylesNames>, MantineMargin>, SharedPopperProps, React.ComponentPropsWithoutRef<'div'> {
    /** Tooltip content */
    label: React.ReactNode;
    /** Any react node that should trigger tooltip */
    children: React.ReactNode;
    /** Tooltip opened state for controlled variant */
    opened?: boolean;
    /** Close delay in ms, 0 to disable delay */
    delay?: number;
    /** Any color from theme.colors, defaults to gray in light color scheme and dark in dark colors scheme */
    color?: MantineColor;
    /** True to disable tooltip */
    disabled?: boolean;
    /** Arrow size in px */
    arrowSize?: number;
    /** Tooltip width in px or auto */
    width?: number | 'auto';
    /** Allow multiline tooltip content */
    wrapLines?: boolean;
    /** Allow pointer events on tooltip, warning: this may break some animations */
    allowPointerEvents?: boolean;
    /** Get tooltip ref */
    tooltipRef?: React.ForwardedRef<HTMLDivElement>;
    /** Tooltip id to bind aria-describedby */
    tooltipId?: string;
    /** useEffect dependencies to force update tooltip position */
    positionDependencies?: any[];
}
export declare function Tooltip({ className, label, children, opened, delay, gutter, color, disabled, withArrow, arrowSize, position, placement, transition, transitionDuration, zIndex, transitionTimingFunction, width, wrapLines, allowPointerEvents, positionDependencies, tooltipRef, tooltipId, classNames, styles, sx, ...others }: TooltipProps): JSX.Element;
export declare namespace Tooltip {
    var displayName: string;
}
//# sourceMappingURL=Tooltip.d.ts.map