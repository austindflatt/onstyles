import React from 'react';
import { DefaultProps, MantineColor } from '@mantine/styles';
import { TimelineItem, TimelineItemStylesNames } from './TimelineItem/TimelineItem';
export interface TimelineProps extends DefaultProps<TimelineItemStylesNames>, React.ComponentPropsWithoutRef<'div'> {
    /** <Timeline.Item /> components only */
    children: React.ReactNode;
    /** Index of active element */
    active?: number;
    /** Active color from theme */
    color?: MantineColor;
    /** Bullet size in px */
    bulletSize?: number;
    /** Timeline alignment */
    align?: 'right' | 'left';
    /** Line width in px */
    lineWidth?: number;
}
export declare function Timeline({ children, style, active, color, bulletSize, align, lineWidth, classNames, styles, className, sx, ...others }: TimelineProps): JSX.Element;
export declare namespace Timeline {
    var Item: typeof TimelineItem;
    var displayName: string;
}
//# sourceMappingURL=Timeline.d.ts.map