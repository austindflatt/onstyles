import React from 'react';
import { DefaultProps, MantineColor, ClassNames } from '@mantine/styles';
import { TabProps } from '../Tab/Tab';
import type { TabsVariant } from '../Tabs';
import useStyles from './TabControl.styles';
export declare type TabControlStylesNames = Exclude<ClassNames<typeof useStyles>, TabsVariant>;
export interface TabControlProps extends DefaultProps<TabControlStylesNames>, React.ComponentPropsWithoutRef<'button'> {
    active: boolean;
    tabProps: TabProps;
    color?: MantineColor;
    variant?: TabsVariant;
    orientation?: 'horizontal' | 'vertical';
    icon?: React.ReactNode;
    buttonRef?: React.ForwardedRef<HTMLButtonElement>;
}
export declare function TabControl({ className, active, buttonRef, tabProps, color, variant, classNames, styles, orientation, icon: __, ...others }: TabControlProps): JSX.Element;
export declare namespace TabControl {
    var displayName: string;
}
//# sourceMappingURL=TabControl.d.ts.map