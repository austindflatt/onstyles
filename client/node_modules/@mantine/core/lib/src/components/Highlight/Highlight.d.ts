import React from 'react';
import { MantineColor, PolymorphicComponentProps } from '@mantine/styles';
import { SharedTextProps } from '../Text/Text';
interface _HighlightProps extends SharedTextProps {
    /** Substring or an array of substrings to highlight in children */
    highlight: string | string[];
    /** Color from theme that is used for highlighting */
    highlightColor?: MantineColor;
    /** Full string part of which will be highlighted */
    children: string;
}
export declare type HighlightProps<C extends React.ElementType> = PolymorphicComponentProps<C, _HighlightProps>;
declare type HighlightComponent = <C extends React.ElementType = 'div'>(props: HighlightProps<C>) => React.ReactElement;
export declare const Highlight: HighlightComponent & {
    displayName?: string;
};
export {};
//# sourceMappingURL=Highlight.d.ts.map